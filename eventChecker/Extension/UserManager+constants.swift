import Foundation

extension UserManager {
    enum Constants {
        enum UserDefaults {
            static let userList = "userList"
            static let deleteUserList = "deleteUserList"
        }
    }
}
